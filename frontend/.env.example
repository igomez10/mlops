# Local Vite dev: point the browser to the API (different port than the UI).
VITE_API_BASE_URL=http://127.0.0.1:8000

# Production (FastAPI serving static/): set empty so fetches are same origin.
# VITE_API_BASE_URL=
